<!DOCTYPE html>
<html>
  <head>
    <title>SuperAdmin | Chatwoot</title>
    <%= javascript_pack_tag 'superadmin' %>
    <%= stylesheet_pack_tag 'superadmin' %>
  </head>
  <body data-gr-c-s-loaded="true">
    <div id="app" class="superadmin-body app-wrapper app-root">
      <div class="medium column login">
        <div class="text-center medium-12 login__hero align-self-top">
          <img
            src="/brand-assets/logo.svg"
            alt="Chatwoot logo"
            class="hero__logo"
          />
          <h2 class="hero--title">
            Chatwoot + WPP Connect👋
          </h2>
        </div>
        <div class="row align-center">
          <div class="small-12 medium-4 column">
            <%= form_with model: @wpp_connect, url: app_wpp_connect_create_path(
              api_access_token: params[:api_access_token], account_id: params[:account_id]
              ), class: 'login-box column align-self-top' do |form| %>
              <div class="column log-in-form">
                <% if flash[:error].present? %>
                <div data-alert class="alert-box warning"><%= flash[:error] %></div>
                <% end %>
                <label>
                  <span>Nome</span>
                  <%= form.text_field :name, placeholder: "Whatsapp 1", required: true %>
                </label>
                <%= form.submit "Criar conexão", class: 'button nice large expanded' %>
              </div>
            <% end %>
          </div>
        </div>
    </div>
  </body>
</html>
